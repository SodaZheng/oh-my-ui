---
name: correct-ui-knowledge
description: 将用户的自然语言反馈转成受控的 UI 知识更正，保持既有结构，检查关联场景、通用规则、页面形态和例外是否冲突，并在用户确认后留下版本化记录。适用于 vibe 式纠错、补充规则、声明例外或修正观察事实；不要用于普通查询或首次扫描。
---

# 更正 UI 知识库

开始前完整阅读：

- [共同知识契约](../../references/knowledge-contract.md)
- [UI 描述统一架构](../../references/ui-description-architecture.md)
- [受控更正工作流](../../references/correction-workflow.md)
- [质量门槛](../../references/quality-gates.md)

## 核心原则

- 自然语言反馈不是直接写入指令，先转成结构化更正提案。
- 区分四类变化：观察事实修正、设计规范、限定例外、待验证想法。
- 修改一个组件、场景或页面前必须检查相关组件配方、页面实例、上位通用规则、页面形态索引、业务模块索引和待确认事项。
- 设计偏好不能改写为“历史页面事实”；源码观察也不能自动升级为正式规范。
- 语义变化必须先预览、再由用户明确确认。纯错别字或格式修复可直接处理，但不得改变适用边界和含义。

## 第一阶段：提案与影响分析

1. 只定位目标项目根目录下的 `doc/ui/.ui-knowledge/knowledge.json` 与 `doc/ui/UI设计知识库/`，读取当前 `revision`、导航、目标组件/场景/页面和直接关系；不得读取或更新项目根目录中的旧路径。
2. 将反馈解析为：变化类型、目标对象、原内容、新内容、理由、适用条件、例外范围、证据来源和置信度。
3. 建立影响集：引用该组件的场景配方和页面实例、直接关系场景、共享页面形态的页面、关联通用规则、业务模块和待确认项。
4. 检查至少八类冲突：适用条件重叠、组件组合互斥、组件位置冲突、页面形态不一致、区域层级断裂、状态遗漏、例外范围不清、观察与规范混淆。
5. 向用户给出中文预览：将修改什么、不会修改什么、受影响范围、发现的冲突、建议处理方式和新版本号。
6. 暂停，等待用户明确表达确认、调整或取消。未确认不得改动语义知识。

## 第二阶段：确认后提交

1. 重新读取当前 `revision`。若与预览版本不同，判定为并发变化，重新做影响分析。
2. 在临时位置生成完整候选 `knowledge.json` 和变更记录；不得只做不可追踪的局部文本替换。
3. 保留稳定 ID、未知字段、未涉及场景、历史证据和原始观察；更新所有派生索引。
4. 用 `node <插件根目录>/scripts/commit-kb.mjs <项目根目录> <候选知识文件> <变更记录文件>` 校验并提交。脚本路径相对于本文件向上两级。
5. 重新生成可见文档并运行最终校验。失败时保持旧版本不变，报告失败原因。
6. 告知用户实际变更、冲突处理结果、新 revision 和仍待确认的事项。

## 更正提案最小字段

```json
{
  "id": "CR-YYYYMMDD-NNN",
  "baseRevision": 1,
  "type": "observation-correction | normative-rule | scoped-exception | hypothesis",
  "summary": "中文变化摘要",
  "targets": ["稳定对象 ID"],
  "impactIds": ["受影响对象 ID"],
  "conflicts": [],
  "decision": "confirmed",
  "reason": "用户确认的理由"
}
```

不要把敏感凭证、完整源码、用户会话或无关工程细节写入变更记录。
